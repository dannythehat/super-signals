name: Day 4 Prettier note

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - name: Status
        run: echo "Day 4 stylesheet formatting completed."
