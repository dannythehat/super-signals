name: Cloudflare Pages deployment note

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment method
        run: echo "Cloudflare Pages previews and production deploys are handled by the connected GitHub integration."
